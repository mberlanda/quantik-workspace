# Bounded Context

Context is layered: stable system documents, one repository packet, an initiative/repository task or release, and execution evidence. The CLI lists included/excluded sources, live revisions/versions, and fails if the approximate token budget is exceeded. It never silently scans every source tree.
