# Release Model

Independent axes: repository/package version, contracts repository release, wire ID, action ref, CI expected release, and supported-contract declaration. States progress from planned through candidate, immutable publication, producer verification, consumer adoption, and completed evidence. Source mode never relies on the future tag; published mode tests the exact tag. Exact tags are immutable.
