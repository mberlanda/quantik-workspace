# Decision Context

Stable cross-repository decisions live in `docs/adr/`. Initiative-specific open/resolved decisions live with the task packet. Context bundles include only the relevant decision layer.
