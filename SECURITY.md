# Security Policy

Report vulnerabilities privately through GitHub security advisories for the affected repository. Do not include credentials or private artifacts in task packets, reports, release locks, or fixtures.

Remote writes require explicit execution flags and appropriate credentials. Treat repository commands and compatibility adapters as trusted project code; inspect manifests before execution. Release tags are immutable, checksums must be verified, and pickle/model artifacts must follow their owning repository's trust policy.
