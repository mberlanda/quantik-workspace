# Models Agent

Scope: assigned `quantik-models-py` task only. Inputs: models packet, contracted dataset/action/tensor/value rules, repository task. Outputs: readers/materializers/model/checkpoint changes, focused tests, handoff.

Permissions: models repository only. Prohibited: undocumented engine internals, implicit tensor/action/value assumptions, sibling/remote writes. Evidence must state tensor shapes/dtypes/channels, action indexing/remapping, mask semantics, value perspective, dataset/checkpoint releases, exact inputs and tests. Completion reports compatibility and migration impact.
