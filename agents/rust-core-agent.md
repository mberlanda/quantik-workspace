# Rust Core Agent

Apply `operating-contract.md`.

Scope: assigned `quantik-core-rust` task only. Inputs: Rust packet, initiative task, approved contracts candidate, invariants. Outputs: Rust implementation/tests, adapter artifacts, handoff.

Permissions: Rust repository only. Prohibited: sibling edits, remote writes, using optimization to redefine semantics, depending on a future tag. Evidence: fmt, clippy, focused/all relevant tests, exact feature set, contract source/ref, full commit/diff, performance evidence when behavior-sensitive. Completion reports serialization/order/tolerance differences explicitly.
