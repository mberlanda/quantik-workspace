# QW-002 Decisions

Open decisions:

1. TOML, YAML, or typed Python configuration?
2. Which stages are mandatory, optional, resumable, or cacheable?
3. What is the precedence of profile, environment, and command-line overrides?
4. How are sibling revisions and effective parameters captured in run output?
5. Which legacy scripts remain as compatibility wrappers during migration?
