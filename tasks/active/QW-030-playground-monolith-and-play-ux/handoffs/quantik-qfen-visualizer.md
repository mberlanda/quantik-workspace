# Handoff

## V2 — Modes of play

- Initiative / repository: QW-030 / `quantik-qfen-visualizer`
- Branch and full commit: `feat/mode-chooser` @ `5733f7a`, **stacked on `feat/lab-drawer` (V1, #8)** — both touch `app.js` in the same neighborhood, and the packet calls for sequencing rather than parallelizing them. Not merged — PR opened, merge is the user's call, and its diff will not be clean until #8 merges first.
- Dirty-state before/after: clean before and after.
- PR: https://github.com/mberlanda/quantik-qfen-visualizer/pull/9 (base `feat/lab-drawer`; currently shows #8's commits too, until #8 lands)
- Files changed (across this item's two commits, `c2f0b5f` and `5733f7a`, both within V2's `allowed_paths` except the flagged deviation below):
  - `src/modes.js` (new) — `MODES` (4 entries: `play-a-model`, `watch-two-engines`, `two-players`, `just-the-board`, each with `label` and one-line `description`), `DEFAULT_MODE_ID` (`"just-the-board"`), and pure `applyMode(profile, modeId)`. Three modes return a fixed `{player0, player1}` controller assignment; `"just-the-board"` (and any unrecognized id, which resolves to it) passes the given `profile.player0`/`profile.player1` straight through, falling back to `human`/`tactical` when neither is given.
  - `test/modes.test.js` (new) — 7 tests: the 4 `MODES` entries and their shape, each real mode's assignment, the pass-through behavior with and without input, the unknown-id fallback (including that its pass-through behavior matches `"just-the-board"` exactly), and the `mode` field round-tripping through `settings.js`'s `writeProfile`/`readProfile`.
  - `src/settings.js` — `DEFAULT_PROFILE.mode = ""`, `normalizeProfile` gained a `mode` field via a new `normalizeMode` (trim, no validation against `MODES`' ids — deliberately decoupled, see decision below).
  - `test/settings.test.js` — 3 existing `deepEqual` assertions updated to include `mode: ""` (the two "defaults" tests and the round-trip test), since a new profile field appears in every one of them.
  - `index.html` — new `<div id="mode-chooser" role="group" aria-label="Choose how to play"></div>` placed right after the masthead, before the game panel — the first thing on the page below the title. Added `<script src="src/modes.js"></script>` before `app.js`.
  - `src/app.js` — `Modes = global.QuantikModes`; new `selectedMode` module state (initialized from `Settings.readProfile().mode`); `initializeMode()` applies a persisted mode's assignment to the two controller selects before the first render, then calls `renderModeChooser()`; `renderModeChooser()` builds the 4 buttons from `Modes.MODES`, marking the one matching `selectedMode`; `selectMode(modeId)` sets `selectedMode`, applies the new assignment, calls `configureEngines()`, persists via `saveProfile()`, and re-renders; `saveProfile()` now includes `mode: selectedMode`.
  - `test/index.test.js` — **outside V2's `allowed_paths`** (see deviation below): the exact-match script-list assertion needed `"src/modes.js"` added, same as V1's `layout.js` deviation.
- Decisions and assumptions:
  - **O1 → D7 (recorded in `decisions.md`): the chooser persists across visits.** Chosen over resetting to the prompt every time because it matches the profile's existing philosophy (`playerName`, `analysisOpponent`, `serviceBase` all already work this way) and because resetting would nag a returning player every reload. A brand-new visitor still gets the neutral prompt: `DEFAULT_PROFILE.mode` is `""`, which matches none of the four ids, so no button renders selected, and the controller selects keep whatever `index.html` already sets them to — nothing regresses for someone who never touches the chooser at all.
  - **O2 → D8 (recorded in `decisions.md`): a classical-only roster is offered under "Play a model," not hidden or disabled.** `applyMode` unconditionally sets `player1` (or whichever seat) to the `"service"` controller; the opponent picker already shows whatever `GET /api/opponents` returns, with no special-casing needed for "no staged models" — it already degrades to the six classical engines on its own. Zero new branches, and it matches `initiative.md`'s explicit "must read as a deliberate state, not a failure" language.
  - `player0`/`player1` in `applyMode`'s `profile` parameter is **not** the `settings.js` profile object — it's a lightweight `{player0, player1}` shape describing the *current controller selection*, built fresh in `app.js` from the live select values (`currentControllers()`). Kept separate from the persisted settings profile (which only stores the *chosen mode id*, not the resulting controllers) so the two concerns — "what mode did you pick" and "what controllers does that imply right now" — don't get tangled.
  - Manually changing a controller select in the drawer after picking a mode does **not** clear the mode highlight — the chooser keeps showing the last-clicked mode even if the drawer has since overridden it. Not addressed here; a plausible follow-up if it reads as misleading once V4's styling makes the chooser more prominent.
- **Scope deviation (flagging per "path expansion requires coordinator review"):** `test/index.test.js` was touched again, outside V2's `allowed_paths` (`index.html, src/app.js, src/modes.js, src/settings.js, test/modes.test.js, test/settings.test.js`) — same one-line addition (`"src/modes.js"`) to the same exact-match script list, for the same reason as V1's `layout.js`. Flagged in both PRs; will very likely recur for V3's `rules.js` too.
- Commands and exact results:
  - `npm test` → `77 passed` (69 from #8 + 8 new: 6 in `test/modes.test.js` plus 2 more once the settings assertions are counted individually — see the PR for the exact breakdown), 0 failures.
  - Rendered `index.html` via `python3 -m http.server 5175` and drove it with Claude in Chrome: fresh load shows four unhighlighted mode buttons at the top; clicking "Two players" highlights it and flips both drawer controller selects to "Human" (confirmed by opening the drawer); reloading the page kept "Two players" highlighted and the board still showed "Player 0 · Human" — the persisted mode round-tripped through `localStorage` in a real browser; clicking "Just the board" afterward highlighted it in turn while leaving the controllers at Human/Human exactly as "Two players" had left them, confirming the pass-through behavior live and not only in the unit test.
- Generated evidence: no CI workflow exists in this repo; the browser-interaction notes above are the rendering evidence in lieu of a CI artifact or saved screenshot files.
- Known gaps / follow-up: the mode highlight can go stale relative to the actual controllers if the drawer is used to override them afterward (see decision note above) — not a regression from before V2 (there was no highlight to go stale), but worth a look if it confuses anyone once V4 makes the chooser visually prominent.
- Prohibited or unperformed remote actions: PR opened, not merged, per the standing instruction to ask before merging any `quantik-qfen-visualizer` PR.

## V1 — The lab drawer

- Initiative / repository: QW-030 / `quantik-qfen-visualizer`
- Branch and full commit: `feat/lab-drawer` @ `8880327cd125157dff23e98508bfd5cc357c6d55`, based on `main` at `6ae703d5f47b2273055aab54ee92cf8252391647`. Not merged — PR opened, merge is the user's call.
- Dirty-state before/after: clean before and after.
- PR: https://github.com/mberlanda/quantik-qfen-visualizer/pull/8
- Files changed (both commits, both within V1's `allowed_paths` except the flagged deviation below):
  - `src/layout.js` (new) — `ADVANCED_CONTROL_IDS` (ordered list of the 13 ids moved into the drawer: `qfen-input`, `copy-button`, `reset-button`, `seed-input`, `speed-select`, `player-0-controller`, `player-1-controller`, `remote-endpoint-0`, `remote-endpoint-1`, `service-base`, `export-button`, `import-button`, `import-file`), `isAdvancedControl(id)`, and `readDrawerOpen`/`writeDrawerOpen` — localStorage helpers for the drawer's open state, same defensive try/catch shape as `settings.js`'s theme storage (wraps `getItem` too, so a storage that throws on read is also handled, not just one that returns null).
  - `test/layout.test.js` (new) — 6 tests: the id list contains every moved control, the predicate is true only for those ids, default-closed with no stored value, a persisted open state survives a reload, a corrupt stored value (`"{not json"`) falls back to closed without throwing, and storage that throws on both `getItem`/`setItem` is tolerated.
  - `index.html` — the QFEN form (input + Copy/Reset + its error message), the seed/speed/controller controls, both remote endpoint fields, the service-base override (kept as its own nested `<details class="endpoint-override">`, unchanged apart from location), and Export/Import trace (+ the hidden `import-file` input) all moved into one new `<details id="advanced-drawer"><summary>Advanced</summary>...</details>` at the bottom of the page, collapsed by default. Every element id kept unchanged. Added `<script src="src/layout.js"></script>` before `app.js`.
  - `src/app.js` — added `Layout = global.QuantikLayout`, added `advanced-drawer` to the queried element ids, and on init sets `elements.advancedDrawer.open = Layout.readDrawerOpen()` then persists on the drawer's native `toggle` event via `Layout.writeDrawerOpen(...)`.
  - `test/index.test.js` — **outside V1's `allowed_paths`** (see deviation below): its exact-match script-list assertion needed `"src/layout.js"` added.
- Decisions and assumptions:
  - Labeled the drawer "Advanced" (not "Settings", not "Lab" — avoided reusing "Lab" since the page is already titled "Game Lab" and a second use felt confusing).
  - `error-message` (the QFEN validation message) moved along with `qfen-input` even though the packet's list doesn't name it explicitly — it's tightly coupled to that field (`aria-invalid` is set on the textarea on parse failure) and leaving a naked error paragraph on the main surface with no visible input to explain it seemed worse than moving it with its control.
  - `player-name` and `opponent-0`/`opponent-1` (the trained-model pickers) were **not** moved — the packet's list names only the two controller selects (`player-0-controller`/`player-1-controller`), not the model pickers, which read as more central to "Play a model" mode and likely belong on V2's main surface rather than the drawer.
  - The nested "Play service endpoint" `<details>` around `service-base` was kept as-is (just relocated) rather than flattened, since collapsing it into the outer drawer's already-collapsed content seemed like unnecessary restructuring for a relocation task; V4 (styling) can revisit if a nested disclosure inside a disclosure reads badly once styled.
- **Scope deviation (flagging per "path expansion requires coordinator review"):** `test/index.test.js` was touched, outside V1's `allowed_paths` (`index.html, src/app.js, src/layout.js, test/layout.test.js`). Necessary, not optional: that test asserts the exact ordered list of `<script>` tags in `index.html` via `assert.deepEqual`, and adding `src/layout.js` as a new classic script (required for `app.js` to reach `QuantikLayout` at runtime — the whole point of the module split) changes that list. One line changed (added `"src/layout.js"` to the expected array). Following the pattern from `quantik-models-py` M1 (see its handoff entry) rather than leaving the suite red or duplicating `layout.js`'s logic inline in `app.js` to avoid the script tag. This same conflict will very likely recur for V2 (`modes.js`) and V3 (`rules.js`), which have the identical `index.html`-plus-new-module shape and also don't list `test/index.test.js` in their `allowed_paths`.
- Commands and exact results:
  - `npm test` → `69 passed` (63 existing + 6 new), 0 failures.
  - Rendered `index.html` via `python3 -m http.server 5175` and drove it with Claude in Chrome: confirmed the main surface no longer shows the QFEN box, controller matrix, or endpoint fields (game controls, theme settings, examples, board, and analysis panel all render as before); scrolled to the bottom and confirmed a collapsed "▶ Advanced" disclosure; opened it and confirmed the QFEN textarea + Copy/Reset, both controller selects, seed, speed, both remote endpoint fields, the nested service-endpoint override, and Export/Import trace are all present and wired; reloaded the page and confirmed the drawer stayed open — proof the `localStorage` round-trip works in a real browser, not only against the DOM stub `node --test` uses.
- Generated evidence: no CI workflow exists in this repo (confirmed `.github/workflows` absent), so no CI run to cite; screenshots are in the PR description's rendering notes rather than attached images (this pass used live browser interaction, not saved screenshot files).
- Known gaps / follow-up: `player-name` and `opponent-0`/`opponent-1` remain on the main surface, outside both the drawer and (until V2 lands) any prominent placement — currently sitting in a mostly-emptied `remote-controls` block on the game panel. V2's mode chooser is expected to give them a proper home.
- Prohibited or unperformed remote actions: PR opened, not merged, per the standing instruction to ask before merging any `quantik-qfen-visualizer` PR.
