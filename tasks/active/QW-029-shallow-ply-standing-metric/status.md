# QW-029 Status

**plan-required.** Scaffolded by `task create` on 2026-08-31 and never filled
in: `manifest.yaml` still carries the generated defaults (`problem: "To be
refined."` and the boilerplate acceptance criterion), `initiative.md` is the
generated stub, and no decisions are recorded.

Per [`tasks/README.md`](../../README.md), `plan-required` is the honest state
for this packet — it is pickable for **plan generation and review only**, and
must not be picked up for implementation until a problem statement and an
approved plan revision are recorded. It was previously marked `planned`, which
asserts the opposite.

## Not yet written

The problem statement, the acceptance criteria, and the relationship to the
adjacent shallow-ply work — [`QW-027`](../QW-027-corpus-label-structure/initiative.md)
(the shallow-ply floor), [`QW-023`](../QW-023-corpus-axis-at-converged-budget/initiative.md)
(the corpus axis at a converged budget), and
[`ADR 0014`](../../../docs/adr/0014-corpus-coverage-and-epoch-budget-are-separate-axes.md).
Those are adjacent, not a substitute: none of them defines what this packet is
for, and the scope has not been inferred from them deliberately.

## Next action

Write the problem statement, then the plan. Nothing else should happen first.
