{
  "acceptance_criteria": [
    "Hugging Face dataset repositories brpoplpush/quantik-dev-data and brpoplpush/quantik-dev-runs exist and carry all nine staged artefact groups, LFS-tracked from the first commit \u2014 a file committed as a plain blob cannot be fixed by a later commit.",
    "Every file under runs/ is covered by exactly one group across the two repos, verified by diffing runs/ against the staged MANIFEST.json.",
    "Each group's README states what it is, what it cost, how to reproduce it and how to extend it, and the probe's card leads with HELD OUT.",
    "MANIFEST.json is present and a spot-check confirms at least one downloaded file's sha256 matches it.",
    "A restore is demonstrated end to end into a directory that does not already have runs/ \u2014 download, copy back, and run one training smoke test against the restored corpus.",
    "The licence is recorded on both repos as CC BY-NC 4.0 for data, MIT for the code that produced it, matching the model repositories.",
    "docs/dev-data.md and the generated dataset card name the real namespace rather than <namespace>."
  ],
  "affected_contracts": [],
  "affected_repositories": [
    "quantik-models-py"
  ],
  "created": "2026-08-30",
  "id": "QW-025",
  "problem": "runs/ is gitignored, is about 1.3 GB, and lives on one machine. It holds days of exact-solver time, hours of training, the held-out probe every evaluation number is measured against, and the arena output behind every published claim. Losing that directory means recomputing all of it. The published model repositories do not cover it: a model repo carries weights and a card, not the corpus the weights were fitted to or the probe they were scored against. Staging tooling now exists (models-py #57) and nothing has been uploaded.",
  "status": "published-and-restore-verified",
  "title": "Publish the dev-data dataset repos",
  "dependencies": []
}
