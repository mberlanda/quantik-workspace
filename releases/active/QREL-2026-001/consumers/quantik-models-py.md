# quantik-models-py adoption

Update the live checkpoint default and inline fixtures from contracts 1.1.0 to 1.2.0, add an explicit supported-contract declaration, and pin repository inputs instead of mutable default branches. Retain historical plan values. Validate tensor/action/value semantics, legal-mask meaning, checkpoint round-trip, E2E pipeline, and compatibility evidence.
